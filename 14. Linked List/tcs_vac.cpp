#include<bits/stdc++.h>
using namespace std;
int main(){
	int center_n;
	cin >> center_n;
	vector<int> distance;
	for(int i=0; i<center_n; i++){
		int x,y;
		cin>>x>>y;
		int z = abs(a-b);
		int max;
		int c;
		for(int j=0; i<distance.size(); i++){
			if
		}
		distance.push_back(z);
	}
	int person;
	cin >> person;
	vector<vector<string,pair<int,char>>> m;
	for(int i=0;i<person; i++){
		string name;
		cin >> name;
		int age;
		char category;
		int x,y;
		cin >> age >> category >> x >> y;
		int z = abs(x-y);
		int diff = INT_MAX;
		int _centerN = 0;
		for(int i=0; i<distance.size();i++){
			if(diff>z-distance[i]){
				diff = min(diff,z-distance[i]);
				
			}
		}
	}
}